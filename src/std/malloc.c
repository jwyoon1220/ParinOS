/*
 * src/std/malloc.c — stub
 *
 * The kernel heap allocator has been rewritten in C++ using TlsfAllocator.
 * All implementation is in:
 *   src/std/malloc.cpp  — kmalloc / kfree / kcalloc / krealloc / init_heap
 *
 * This file is intentionally empty to avoid duplicate symbol errors.
 */
